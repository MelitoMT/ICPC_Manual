En geometría computacional, necesitaremos una forma de representar a los objetos geométricos en código, a los puntos del plano los representaremos como un par de coordenadas de $(x,y)$ con las operaciones usuales aplicadas coordenada a coordenada, un punto y un vector son intercambiables cuando el contexto lo permite.

\lstinputlisting[language=C++, firstline=4, lastline=20]{./tex/geo/codes/geo.cpp}
Como definimos nuestra propia estructura es necesario que también definamos los comparadores para poder decidir si dos puntos son iguales o no.
\lstinputlisting[language=C++, firstline=22, lastline=27]{./tex/geo/codes/geo.cpp}
Nótese que el definir un comparador para decidir si un punto es ''menor'' a otro, no es tan fácil qué hacer por lo que usualmente dependiendo el caso los ordenaremos, frecuentemente nos convendrá ordenarlos de forma horaria o antihoraria de acuerdo a los vectores que corresponden los puntos, la pregunta ahora consiste en ¿cómo hacer esto?, antes de ver una técnica para hacerlo, recordaremos algunas operaciones que podemos hacer entre puntos.
\section{Bases}
En primer lugar tenemos el producto punto, dados dos puntos este nos ayuda a medir qué tanto dos vectores apuntan a la misma dirección, si el producto punto es positivo, los vectores apuntan a direcciones similares, si es cero, los vectores son perpendiculares y si es negativo, entonces los vectores apuntan en direcciones opuestas, en programación competitiva este se calcula simplemente con las coordenadas.
\lstinputlisting[language=C++, firstline=38, lastline=40]{./tex/geo/codes/geo.cpp}
Nótese que por como lo calculamos si hacemos el producto punto de un vector consigo mismo tendremos la norma de este al cuadrado.
\\\\Otro concepto que nos será relevante recordar es el producto cruz entre dos vectores (denotado $a \times b$), este nos dará el área orientada del paralelogramo que forman dos vectores, con orientada nos referimos a que si calculamos $a \times b$ será el área del paralelogramo negativa, en cambio $b \times a$ nos la dará positiva, esto nos da una noción de si un vector está a la ''izquierda'' o ''derecha'' de otro.
\begin{center}
\includegraphics[scale=.5]{./tex/geo/pics/cross1.png}
\end{center}
la implementación también es sencilla y nos da una forma rápida de comprobar si tres puntos son colineales.
\lstinputlisting[language=C++, firstline=42, lastline=44]{./tex/geo/codes/geo.cpp}
Con lo anterior ya tenemos las herramientas suficientes para resolver nuestro problema de ordenar los puntos del plano de forma horaria o antihoraria alrededor de un punto.
\\\\
Supongamos que queremos ordenar los puntos del plano de forma antihoraria respecto al origen y con una referencia de ''inicio'' que tomaremos como $v=(0,1)$.
\\\\Notemos que si tenemos un conjunto de puntos $A$, podemos dividir el plano en dos partes, la primera que llamaremos $N$ que tendrá a todos los puntos que estén a la izquierda de $v$ y todos los puntos que sean colineales apuntando a la misma dirección, la segunda se llamará $S$ y serán los restantes, es decir, aquellos a la derecha de $v$ y los colineales que apuntan en dirección opuesta es decir, los puntos en el área roja estarían en $N$ y los del área azul en $S$.
\begin{center}
\includegraphics[scale=.5]{./tex/geo/pics/sort.png}
\end{center}
Entonces de aquí que digamos que para un punto $x$ en $A$, $x$ está en $N$ si y sólo si se cumple que $v\times x >0$ o $v\cdot x >0$ y además $v\times x=0$, estás siendo las operaciones de producto punto y cruz que vimos arriba, con esto podemos hacer una función que nos verifique si un punto de $A$ está o no en $N$ facilmente
\lstinputlisting[language=C++, firstline=29, lastline=32]{./tex/geo/codes/geo.cpp}
Una vez dividido en dos partes notemos que dados $x$ y $y$, podemos decir que $x < y$ si $x \in N$ y $y \in S$ o ambos están en la misma parte y además $x \times y >0$, para esto entonces sólo nos basta definir un operador como sigue que es más fácil de manipular, nótese entonces que este ordenamiento tiene complejidad $O(n log n)$ donde $n$ es la cantidad de puntos en $A$.
\lstinputlisting[language=C++, firstline=34, lastline=36]{./tex/geo/codes/geo.cpp}
\section{Intersecciones}
En muchas ocasiones nos interesará saber si dados dos segmentos $AB$ y $CD$ estos se intersectan, existen varias formas de revisar esto; sin embargo para evitarnos los muchos casos extremos que pueden aparecer en otros métodos, lo que haremos será pensar que en lugar de segmentos, tenemos dos líneas, la primera parametrizada como $A+t(B-A)$ y la segunda parametrizada como $(x-C)\cdot(D-C)^{\perp}=0$\footnote{Recordemos que dado un vector $(x,y)$, $(x,y)^\perp = (-y,x)$, en este caso sería $(y_C-y_D,x_D-x_C)$}, estas se van a intersecar en un solo punto siempre que no sean paralelas, lo que podemos revisar facilmente verificando que el producto cruz sea diferente a $0$, entonces, si no son paralelas, buscaremos el punto de intersección, es decir, podemos sustituir la primera recta en la segunda y obtener $(A+t(B-A)-C)\cdot(D-C)^{\perp}=0$ de donde podemos despejar $t$
\\
\[t=\frac{(C-A)\cdot(D-C)^\perp}{(B-A)\cdot(D-C)^\perp}\]\\
para sustituirlo en $A+t(B-A)$ y obtener el punto de  intersección y finalmente checar si este punto está sobre cada uno de los segmentos.
\\\\\textbf{Ejercicio} Implementar  una función booleana que reciba cuatro puntos $A,B,C$ y $D$ y que nos diga si dos segmentos $AB$ y $CD$ se intersecan.
\vspace{-1cm}
\section{Polígonos}
Cuando somos más pequeños, se nos enseña que el área de un triángulo se calcula como ''base por altura sobre dos'', imaginemos que solamente nos dan las coordenadas de los tres puntos que definen un triángulo, para usar lo que nos enseñaron tendríamos que hacer varios calculos para encontrar puntos medios y distancias, nos gustaría encontrar una forma más directa que además se pueda generalizar para polígonos en general.
\\Supongamos tenemos $P_1=(x_1,y_1)$, $P_2=(x_2,y_2)$, y $P_3=(x_3,y_3)$, los tres puntos que delimitan un triángulo, notemos que si consideramos al origen $(0,0)$ entonces el area buscada equivale a sumar el área de los triángulos $P_3OP_2$ y $P_2OP_1$ y restarles el área del triángulo $P_3OP_1$.
\begin{figure}[H]
\centering
\begin{tikzpicture}[scale=.5]
  \coordinate (O)  at (0, 0);
  \coordinate (P1) at (2.5, 5.5);
  \coordinate (P2) at (6.5, 5.8);
  \coordinate (P3) at (8.0, 1.0);
  \fill[green!15]  (O) -- (P1) -- (P2) -- cycle;
  \fill[blue!10]   (O) -- (P2) -- (P3) -- cycle;
  \fill[orange!15] (O) -- (P3) -- (P1) -- cycle;
  \draw[green!50!black, thin] (O) -- (P1) -- (P2) -- cycle;
  \draw[blue!60!black,  thin] (O) -- (P2) -- (P3) -- cycle;
  \draw[red!40!black,   thin] (O) -- (P3) -- (P1) -- cycle;
  \draw[black, thick] (P1) -- (P2) -- (P3) -- cycle;
  \draw[gray, thin, dashed] (O) -- (P1);
  \draw[gray, thin, dashed] (O) -- (P2);
  \draw[gray, thin, dashed] (O) -- (P3);
  \draw[gray!50, thin, ->] (-0.5, 0) -- (9.0, 0) node[right, font=\small] {$x$};
  \draw[gray!50, thin, ->] (0, -0.5) -- (0, 7.0) node[above, font=\small] {$y$};
  \filldraw[black] (O)  circle (2pt) node[below left]  {$O$};
  \filldraw[black] (P1) circle (2pt) node[above left]  {$P_1$};
  \filldraw[black] (P2) circle (2pt) node[above right] {$P_2$};
  \filldraw[black] (P3) circle (2pt) node[right]       {$P_3$};
 
\end{tikzpicture}
\label{fig:shoelace}
\end{figure}
Estas áreas de los triángulos ya las sabemos calcular, pues recordemos que el producto cruz entre $P_3$ y $P_2$ nos da el área con signo del paralelogramo que forman y al dividirlo entre $2$, el área del triángulo buscado es de donde deducimos que la formula del área de un triángulo es
\[A(P_1,P_2,P_3)=\frac{|P_1 \times P_2 + P_2 \times P_3 + P_3 \times P_1|}{2}\]
Nótese que no es necesario restar porque el producto cruz ya nos da el área con signo, entonces el signo de menos ya está incluido en la suma.
\\\\Resulta que esta misma lógica funciona para polígonos de $n$ vértices donde se suman $n$ triangulos haciendo que el exterior se cancele, la fórmula generalizada entonces quedaría
\[A(P_1,\cdots,P_n)=\frac{|\sum_{i=1}^{n}P_i \times P_{i+1}|}{2}\]
con $P_{n+1}=P_1$. Esta fórmula funciona también para polígonos no convexos.
\\\\Otro problema que nos podemos encontrar al trabajar con polígonos es dado un punto $Q$ determinar si está afuera o adentro de un polígono dado. El método más usado para resolver este problema se llama ''ray casting'', la idea es que se lanza un rayo horizontal desde $Q$ hacia la derecha y contamos cuántas veces cruza el polígono, si lo cruza  un número impar de veces está adentro, de lo contrario está afuera.
\begin{center}
\vspace{-.6cm}
\begin{tikzpicture}[scale=0.9]
  \begin{scope}[xshift=0cm]
    \coordinate (A1) at (2.0, 4.2);
    \coordinate (A2) at (3.8, 3.2);
    \coordinate (A3) at (3.4, 1.2);
    \coordinate (A4) at (0.6, 1.2);
    \coordinate (A5) at (0.2, 3.2);
 
    \fill[green!12] (A1)--(A2)--(A3)--(A4)--(A5)--cycle;
    \draw[green!60!black, thick] (A1)--(A2)--(A3)--(A4)--(A5)--cycle;
 
    \foreach \v in {A1,A2,A3,A4,A5}
      \filldraw[green!60!black] (\v) circle (2.5pt);
 
    \filldraw[blue!70!black] (1.4, 2.5) circle (4pt);
    \node[blue!70!black, left] at (1.25, 2.65) {\small $Q$};
 
    \draw[->, dashed, blue!70!black, thick] (1.4, 2.5) -- (4.4, 2.5);
 
    \filldraw[red!70!black] (3.55, 2.5) circle (4pt);
    \node[red!70!black, above] at (3.55, 2.62) {\small $1$};
 
    \node at (2.0, 0.5) {\small 1 cruce $\to$ impar};
    \node[draw, fill=green!10, rounded corners=4pt,
          inner sep=4pt, font=\small] at (2.0, -0.1) {$Q$ está adentro};
  \end{scope}
  \begin{scope}[xshift=6cm]
    \coordinate (B1) at (2.0, 4.2);
    \coordinate (B2) at (3.8, 3.2);
    \coordinate (B3) at (3.4, 1.2);
    \coordinate (B4) at (0.6, 1.2);
    \coordinate (B5) at (0.2, 3.2);
 
    \fill[green!12] (B1)--(B2)--(B3)--(B4)--(B5)--cycle;
    \draw[green!60!black, thick] (B1)--(B2)--(B3)--(B4)--(B5)--cycle;
 
    \foreach \v in {B1,B2,B3,B4,B5}
      \filldraw[green!60!black] (\v) circle (2.5pt);
 
    \filldraw[red!60!black] (-0.6, 2.5) circle (4pt);
    \node[red!60!black, left] at (-0.75, 2.65) {\small $Q$};
 
    \draw[->, dashed, red!60!black, thick] (-0.6, 2.5) -- (4.4, 2.5);
 
    \filldraw[red!70!black] (0.28, 2.5) circle (4pt);
    \node[red!70!black, above] at (0.28, 2.62) {\small $1$};
 
    \filldraw[red!70!black] (3.55, 2.5) circle (4pt);
    \node[red!70!black, above] at (3.55, 2.62) {\small $2$};
 
    \node at (2.0, 0.5) {\small 2 cruces $\to$ par};
    \node[draw, fill=red!8, rounded corners=4pt,
          inner sep=4pt, font=\small] at (2.0, -0.1) {$Q$ está afuera};
  \end{scope}
 
\end{tikzpicture}
\end{center}
este es un método sencillo para problemas donde sabemos con certeza no pasarán casos extremos, ya que al lanzar el rayo podría suceder lo siguiente
 
\begin{center}
\begin{tikzpicture}[scale=0.9]
  \begin{scope}[xshift=0cm]
    \coordinate (R1) at (2.0, 4.5);
    \coordinate (R2) at (4.0, 2.5);
    \coordinate (R3) at (2.0, 0.5);
    \coordinate (R4) at (0.0, 2.5);
 
    \fill[green!12] (R1)--(R2)--(R3)--(R4)--cycle;
    \draw[green!60!black, thick] (R1)--(R2)--(R3)--(R4)--cycle;
 
    \foreach \v in {R1,R2,R3,R4}
      \filldraw[green!60!black] (\v) circle (2.5pt);
 
    \filldraw[blue!70!black] (1.2, 2.5) circle (4pt);
    \node[blue!70!black, above left] at (1.1, 2.6) {\small $Q$};
 
    \draw[->, dashed, blue!70!black, thick] (1.2, 2.5) -- (4.9, 2.5);
 
    \filldraw[red!70!black] (4.0, 2.5) circle (4.5pt);
    \node[red!70!black, above] at (4.0, 2.7) {\small $?$};
 
    \node[red!60!black] at (2.0, -0.2) {\small \textbf{¿1 cruce o 2 cruces?}};
    \node[gray]         at (2.0, -0.75) {\small rayo pasa por un vértice};
    \node[draw, fill=yellow!10, rounded corners=4pt,
          inner sep=4pt, font=\small] at (2.0, -1.35) {ambiguo};
  \end{scope}
  \begin{scope}[xshift=6.5cm]
    \coordinate (H1) at (0.8, 4.2);
    \coordinate (H2) at (2.8, 4.2);
    \coordinate (H3) at (3.8, 2.5);
    \coordinate (H4) at (2.2, 2.5);
    \coordinate (H5) at (1.8, 0.6);
    \coordinate (H6) at (0.2, 2.5);
 
    \fill[green!12] (H1)--(H2)--(H3)--(H4)--(H5)--(H6)--cycle;
    \draw[green!60!black, thick] (H1)--(H2)--(H3)--(H4)--(H5)--(H6)--cycle;
 
    \draw[red!60!black, ultra thick] (H4)--(H3);
 
    \foreach \v in {H1,H2,H3,H4,H5,H6}
      \filldraw[green!60!black] (\v) circle (2.5pt);
 
    \filldraw[blue!70!black] (-0.7, 2.5) circle (4pt);
    \node[blue!70!black, above left] at (-0.8, 2.6) {\small $Q$};
 
    \draw[->, dashed, blue!70!black, thick] (-0.7, 2.5) -- (4.7, 2.5);
 
    \node[red!60!black] at (1.8, -0.2) {\small \textbf{¿cuántos cruces con la arista?}};
    \node[gray]         at (1.8, -0.75) {\small arista horizontal};
    \node[draw, fill=yellow!10, rounded corners=4pt,
          inner sep=4pt, font=\small] at (1.8, -1.35) {ambiguo};
  \end{scope}
 
\end{tikzpicture}
\end{center}
Para resolver este problema, usamos un algoritmo que se llama ''winding number'', consiste en la misma idea de lanzar un rayo y contar intersecciones, pero en este se va a contar su orientación, nótese que en la primera la arista que intersecta va subiendo entonces se suma $1$, en el segundo caso, se resta $1$ porque la arista va bajando, si el resultado es diferente de $0$, el punto está adentro.
\begin{center}
\begin{tikzpicture}[scale=0.9]
  \begin{scope}[xshift=0cm]
    \coordinate (A1) at (2.0, 4.2);
    \coordinate (A2) at (3.8, 3.2);
    \coordinate (A3) at (3.4, 1.2);
    \coordinate (A4) at (0.6, 1.2);
    \coordinate (A5) at (0.2, 3.2);
 
    \fill[green!12] (A1)--(A2)--(A3)--(A4)--(A5)--cycle;
    \draw[green!60!black, thick] (A1)--(A2)--(A3)--(A4)--(A5)--cycle;
 
    \foreach \v in {A1,A2,A3,A4,A5}
      \filldraw[green!60!black] (\v) circle (2.5pt);
 
    \filldraw[blue!70!black] (1.4, 2.5) circle (4pt);
    \node[blue!70!black, left] at (1.25, 2.65) {\small $Q$};
 
    \draw[->, dashed, blue!70!black, thick] (1.4, 2.5) -- (4.6, 2.5);
 
    \draw[->, green!60!black, very thick] (3.55, 1.6) -- (3.55, 3.0);
    \filldraw[red!70!black] (3.55, 2.5) circle (4pt);
    \node[green!60!black] at (3.9, 2.8) {\small $+1$};
 
    \node[gray] at (2.0, 0.5) {\small winding $= +1 \neq 0$};
    \node[draw, fill=green!10, rounded corners=4pt,
          inner sep=4pt, font=\small] at (2.0, -0.1) {$Q$ está adentro};
  \end{scope}
 
  \begin{scope}[xshift=6.2cm]
    \coordinate (B1) at (2.0, 4.2);
    \coordinate (B2) at (3.8, 3.2);
    \coordinate (B3) at (3.4, 1.2);
    \coordinate (B4) at (0.6, 1.2);
    \coordinate (B5) at (0.2, 3.2);
 
    \fill[green!12] (B1)--(B2)--(B3)--(B4)--(B5)--cycle;
    \draw[green!60!black, thick] (B1)--(B2)--(B3)--(B4)--(B5)--cycle;
 
    \foreach \v in {B1,B2,B3,B4,B5}
      \filldraw[green!60!black] (\v) circle (2.5pt);
 
    \filldraw[red!60!black] (-0.5, 2.5) circle (4pt);
    \node[red!60!black, left] at (-0.6, 2.65) {\small $Q$};
 
    \draw[->, dashed, red!60!black, thick] (-0.5, 2.5) -- (4.6, 2.5);
 
    \draw[->, orange!80!red, very thick] (0.28, 3.1) -- (0.28, 1.6);
    \filldraw[blue!60!black] (0.28, 2.5) circle (4pt);
    \node[orange!80!red] at (-0.1, 2.2) {\small $-1$};
 
    \draw[->, green!60!black, very thick] (3.55, 1.6) -- (3.55, 3.0);
    \filldraw[red!70!black] (3.55, 2.5) circle (4pt);
    \node[green!60!black] at (3.9, 2.8) {\small $+1$};
 
    \node[gray] at (2.0, 0.5) {\small winding $= -1 + 1 = 0$};
    \node[draw, fill=red!8, rounded corners=4pt,
          inner sep=4pt, font=\small] at (2.0, -0.1) {$Q$ está afuera};
  \end{scope}
 
\end{tikzpicture}
\end{center}
 
\vspace{4pt}
{\small
\textcolor{green!60!black}{$+1$} \quad arista cruza el rayo de abajo hacia arriba\\
\textcolor{orange!80!red}{$-1$} \quad arista cruza el rayo de arriba hacia abajo
}
\\\\
Nótese que la dirección de cada arista que cruza el rayo se determina en dos pasos: 
primero verificamos que los extremos de la arista estén en lados opuestos del nivel 
$y = y_Q$ usando las condiciones sobre las coordenadas $y$, y luego usamos 
$\text{prodCruz}(P_{i+1}-P_i,\, Q-P_i)$ para saber si $Q$ queda a la izquierda o 
derecha de la arista, lo que determina si el cruce suma $+1$ o $-1$. 
\\\\Ahora, el lector podrá preguntarse qué sucede con el caso donde el rayo intersecta 
exactamente en un vértice $P_i$. En este algoritmo las condiciones para que una arista 
cuente son asimétricas: para la arista $P_{i-1}P_i$ que \textit{llega} a $P_i$ se requiere
\[y_{P_{i-1}} \leq y_Q < y_{P_i}\]
como $y_{P_i} = y_Q$, la condición estricta $<$ no se satisface y la arista \textbf{no cuenta}. 
Para la arista $P_iP_{i+1}$ que \textit{sale} de $P_i$ se requiere
\[y_{P_i} \leq y_Q < y_{P_{i+1}}\]
como $y_{P_i} = y_Q$, la condición $\leq$ sí se satisface, y la arista \textbf{cuenta} 
únicamente si $y_{P_{i+1}} > y_Q$, es decir, si la arista realmente cruza el rayo hacia 
arriba.
\\\\
El mismo razonamiento elimina las aristas horizontales: si $y_{P_i} = y_{P_{i+1}} = y_Q$, 
ninguna de las dos condiciones se satisface y la arista simplemente no se cuenta, ya explicado el algoritmo, la implementación quedaría:
\lstinputlisting[language=C++, firstline=46, lastline=66]{./tex/geo/codes/geo.cpp}
\section{Convex Hull}
Dado un conjunto de puntos $S \subset \mathbb{R}^2$, su \textbf{cápsula convexa} (\textit{convex hull}) intuitivamente es la figura que se formaría si colocamos una liga estirada alrededor de todos los puntos, más formalmente es el polígono convexo más pequeño que los contiene a todos. En la imagen siguiente $S$ son todos los puntos negros y el hull sería es el polígono dibujado de verde.

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[scale=0.9]
  \filldraw[fill=green!10, draw=green!50!black, thick]
    (0.0, 1.2) -- (0.6, 0.0) -- (2.4, -0.3) -- (4.8, 0.4) --
    (6.0, 2.0) -- (5.6, 3.5) -- (3.6, 3.8) -- (1.2, 3.6) --
    (0.0, 2.8) -- cycle;
 
  \foreach \x/\y in {
    1.7/2.0, 2.8/2.5, 3.2/1.4, 2.2/0.8,
    4.0/2.2, 1.2/1.6, 4.4/1.2, 3.6/2.9,
    2.8/2.0, 2.2/2.9, 4.6/2.6, 1.6/0.9
  }{
    \filldraw (\x,\y) circle (1.8pt);
  }
 
  \foreach \x/\y in {
    0.0/1.2, 0.6/0.0, 2.4/-0.3, 4.8/0.4,
    6.0/2.0, 5.6/3.5, 3.6/3.8, 1.2/3.6, 0.0/2.8
  }{
    \filldraw (\x,\y) circle (1.8pt);
  }
\end{tikzpicture}
 \end{figure}
 
Existen varios algoritmos para calcularlo. El más conocido es \textbf{Graham Scan}; sin embargo, este ordena los puntos por ángulo por lo que hay que tener cuidado con los casos delicados cuando varios puntos tienen el mismo ángulo polar. Por ello preferimos \textbf{Andrew's Monotone Chain}, ambos algoritmos tienen la misma complejidad $O(n \log n)$, misma idea para construir el convex hull, pero ordena por coordenada $x$ en lugar de ángulo, esto lo hace más fácil de implementar sin errores en un concurso.
\\\\Nótese que para saber qué polígono es el hull, sólo tenemos que saber qué puntos forman parte de él y en qué orden. El algoritmo de Andrew construye el convex hull en dos pasadas sobre los puntos ordenados por $x$, primero se construye la parte inferior y luego la superior.
\\\\Para la parte superior se van a recorrer los puntos de izquierda a derecha, y tendremos un vector $hull$ con la caracteristica que los últimos tres puntos siempre van a tener producto cruz es positivo, es decir dados $A,B,C$ tenemos que $(B-A)\times(C-A)>0$, inicialmente se agregan los dos primeros puntos a $hull$ y después cada que lleguemos a un nuevo punto $P$ si este cumple la condición se agrega, de lo contrario, se descarta el tope de la pila y después se agrega $P$ a $hull$. Para la parte inferior es exactamente el mismo procedimiento con la única diferencia que se recorre de derecha a izquierda. La implementación quedaría:
\lstinputlisting[language=C++, firstline=68, lastline=99]{./tex/geo/codes/geo.cpp}
 La desigualdad $<=0$ descarta también los puntos colineales, de modo que el hull resultante solo contiene los vértices extremos. Si el problema requiere incluir los puntos colineales sobre las aristas del hull, podemos hacer la desigualdad estricta.
\\\\\textbf{Ejercicio} Dado un conjunto de $n$ puntos, encontrar el par de puntos más lejanos (diámetro).
\section{Algoritmos de barrido}
El barrido de línea no es por si solo un algoritmo, si no una idea general detrás de varios algoritmos. La idea es mover una línea vertical de izquierda a derecha sobre el plano y procesar los objetos geométricos conforme se van encontrando en lugar de considerar todos a la vez, a esta acción de procesamiento se le llama evento. Es decir, que en cada momento solo nos importa lo que está tocando la línea, para esto es necesario mencionar que la línea no se mueve continuamente si no que salta de evento en evento.
\\\\Estos algoritmos usualmente tienen tres partes:
\begin{itemize}
  \item \textbf{Cola de eventos} esta es una lista ordenada por $x$ de los eventos.
  \item \textbf{Estructura de incidencia} es una estructura que nos dirá qué objetos intersectan la línea en este momento, la elección de la estructura a usar depende el problema, pueden usarse sets, multisets, segment trees, colas, entre otras.
  \item \textbf{Procesamiento de eventos} es la lógica con la que procesaremos cada que ocurra un evento, esta depende mucho de qué problema particular querramos resolver.
\end{itemize}

Propongamos el siguiente problema: Dados $n$ segmentos en el plano, decir si al menos dos de ellos se intersectan.
\\\\Podríamos pensar ingenuamente que la única forma de resolver este problema es revisar todos los pares de segmentos y revisar intersección entre ellos es decir una complejidad de $O(n^2)$, sin embargo, usando barrido de línea podemos resolver este problema más rápido.
\\\\El algoritmo que veremos para resolver este problema se llama $Shamos-Hoey$ y se basa en que si dos segmentos se llegaran a intersectar existe un momento durante el barrido en el que son vecinos en la estructura ordenada por $y$. Esto hace que el problema se reduzca a que en lugar de revisar todos los pares, revisar los vecinos en la estructura cada que suceda un evento.
\\\\Los eventos que podemos tener en este problema son puntos que indican el inicio o final de un segmento, entonces tendremos dos tipos de eventos:
\begin{itemize}
  \item \textbf{Inicio} Insertaremos el punto a la estructura que ya dijimos está ordenada por $y$ y verificaremos si se intersecta con el vecino superior o inferior.
  \item \textbf{Final} Este evento indica el fin de un segmento, entonces lo quitaremos de la estructura, pues ya no nos interesa. Sin embargo, hay que tener cuidado es necesario revisar si los dos vecinos (que al quitar el punto se vuelven adyacentes) se intersecan.
\end{itemize}

 \newcommand{\ejes}{
  \draw[gray!40, thin] (0,0) -- (10.2,0);
  \draw[gray!40, thin] (0,0) -- (0,9.2);
}
\newcommand{\segs}{%
  \draw[green!60!black,  thick] (0,2.5) -- (10,5.5) node[right,font=\tiny] {\tiny S1};
  \draw[blue!70!black,   thick] (1,7.0) -- (7,3.0)  node[right,font=\tiny] {\tiny S2};
  \draw[orange!80!black, thick] (2,1.0) -- (10,4.0) node[right,font=\tiny] {\tiny S3};
  \draw[red!70!black,    thick] (4,8.0) -- (10,3.5) node[right,font=\tiny] {\tiny S4};
}
 
\begin{figure}[H]
\centering
 
\begin{tikzpicture}[scale=0.25]
  \ejes \segs
  \draw[red!70, dashed] (0,0) -- (0,9);
  \fill[green!60!black] (0,2.5) circle (3.5pt);
  \node[font=\tiny] at (5,-1.6) {\texttt{[S1]}};
  \node[font=\tiny]  at (5,10.2) {\textbf{(a)} entra S1};
\end{tikzpicture}
\hfill
\begin{tikzpicture}[scale=0.25]
  \ejes \segs
  \draw[red!70, dashed] (1,0) -- (1,9);
  \fill[green!60!black] (1,2.8)  circle (3.5pt);
  \fill[blue!70!black]  (1,7.0)  circle (3.5pt);
  \draw[yellow!70!black, dashed, thin] (1,2.8) -- (1,7.0);
  \node[font=\tiny] at (5,-1.6) {\texttt{[S1,S2]}};
  \node[font=\tiny]  at (5,10.2) {\textbf{(b)} entra S2};
\end{tikzpicture}
 
\vspace{0.4cm}
 
\begin{tikzpicture}[scale=0.25]
  \ejes \segs
  \draw[red!70, dashed] (2,0) -- (2,9);
  \fill[green!60!black] (2,3.1)  circle (3.5pt);
  \fill[blue!70!black]  (2,6.33) circle (3.5pt);
  \fill[orange!80!black](2,1.0)  circle (3.5pt);
  \draw[yellow!70!black, dashed, thin] (2,1.0) -- (2,3.1);
  \node[font=\tiny] at (5,-1.6) {\texttt{[S3,S1,S2]}};
  \node[font=\tiny]  at (5,10.2) {\textbf{(c)} entra S3};
\end{tikzpicture}
\hfill
\begin{tikzpicture}[scale=0.25]
  \ejes \segs
  \draw[red!70, dashed] (4,0) -- (4,9);
  \fill[green!60!black] (4,3.7)  circle (3.5pt);
  \fill[blue!70!black]  (4,5) circle (3.5pt);
  \fill[orange!80!black](4,1.75) circle (3.5pt);
  \fill[red!70!black]   (4,8.0)  circle (3.5pt);
  \draw[yellow!70!black, dashed, thin] (4,5.67) -- (4,8.0);
  \node[font=\tiny] at (5,-1.6) {\texttt{[S3,S1,S2,S4]}};
  \node[font=\tiny]  at (5,10.2) {\textbf{(d)} entra S4};
\end{tikzpicture}
 
\vspace{0.4cm}
 
\begin{tikzpicture}[scale=0.25]
  \ejes \segs
  \draw[red!70, dashed] (7,0) -- (7,9);
  \fill[green!60!black] (7,4.6)  circle (3.5pt);
  \fill[orange!80!black](7,2.9) circle (3.5pt);
  \fill[red!70!black]   (7,5.75) circle (3.5pt);
  \draw[yellow!70!black, dashed, thin] (7,3.25) -- (7,4.6);
  \node[font=\tiny] at (5,-1.6) {\texttt{[S3,S1,S4]}};
  \node[font=\tiny]  at (5,10.2) {\textbf{(e)} sale S2};
\end{tikzpicture}
\hfill
\begin{tikzpicture}[scale=0.25]
  \ejes \segs
  \draw[red!70, dashed] (10,0) -- (10,9);
  \fill[orange!80!black](10,4.0) circle (3.5pt);
  \fill[red!70!black]   (10,3.5) circle (3.5pt);
  \fill[red!80] (9.5,3.78) circle (4.5pt);
  \draw[yellow!70!black, dashed, thin] (10,3.5) -- (10,4.0);
  \node[font=\tiny] at (5,-1.6) {\texttt{[S3,S4]}};
  \node[font=\tiny]  at (5,10.2) {\textbf{(f)} sale S1, hay interseccion};
\end{tikzpicture}
 
\vspace{0.2cm}
 
\begin{tikzpicture}[scale=0.25]
  \draw[green!60!black,  thick] (0,0)--(2,0) node[right,font=\tiny]{S1};
  \draw[blue!70!black,   thick] (5,0)--(7,0) node[right,font=\tiny]{S2};
  \draw[orange!80!black, thick] (10,0)--(12,0) node[right,font=\tiny]{S3};
  \draw[red!70!black,    thick] (15,0)--(17,0) node[right,font=\tiny]{S4};
  \draw[red!70, dashed]         (20,0)--(22,0) node[right,font=\tiny]{barrido};
  \fill[red!80] (27,0) circle (4pt) node[right,font=\tiny]{cruce};
  \draw[yellow!70!black,dashed,thin] (31,0)--(33,0) node[right,font=\tiny]{revisión};
\end{tikzpicture}
 
\end{figure}
Si en cualquier punto encontramos una intersección, entonces terminamos el algoritmo pues ya resolvimos el problema. Es importante mencionar, que si hay varios eventos en un mismo $x$, primero procesaremos los inicios y depués los finales, así lograremos también comprobar intersecciones si los segmentos comparten vértices.
\\\\La estructura que nos convendrá para este problema es un \texttt{set<pair<point,point>\>>} de C++ ordenado por la coordenada $y$ de cada segmento en la posición actual $x$ de la línea. El comparador para ordenar evalúa para cada segmento
\[y(x) = y_1 + \frac{(y_2 - y_1)(x - x_1)}{x_2 - x_1}\]  al implementarlo hay que ser cuidadosos pues como estaremos trabajando con decimales al hacer comparaciones debemos agregar una tolerancia $\varepsilon$.
\lstinputlisting[language=C++, firstline=111, lastline=213]{./tex/geo/codes/geo.cpp}
Nótese que hay $2n$ eventos y cada evento realiza a lo más dos chequeos de intersección que cada uno tiene complejidad de $O(1)$ y una adición o eliminación con complejidad  $O(log n)$. Además ordenar inicialmente los segmentos tiene complejidad $O(nlogn)$ entonces la complejidad del algoritmo es $O(nlogn)$ que en efecto es más rápido que $O(n^2)$.
\\\\En el problema que resolvimos bastaba con encontrar una intersección, podríamos preguntarnos qué sucede si queremos encontrar todas las intersecciones. El algoritmo que se usa para este caso se llama \textbf{Bentley-Ottmann} que encuentra todas las intersecciones en $O((n+k)logn)$ donde $k$ es el número de intersecciones. Podría parecer que la diferencia es sútil y sólo basta con seguir el algoritmo que estabamos usando antes sin detenerlo, pero recordemos que nuestro algoritmo dependía fuertemente en el orden de los segmentos para encontrar intersecciones, cuando sucede una intersección, el orden de dos segmentos se cambia esto provoca que cada que encontremos un cruce tendremos que cambiar el orden de la estructura y además revisar si los nuevos vecinos tienen cruces. Y no solo eso, si no que cada que agreguemos un segmento si encontramos una intersección tendremos que calcularla y agregarla a la cola de eventos.
\\Mayores complicaciones de Bentley-Ottmann
\begin{itemize}
  \item Cuando dos segmentos cambian de orden, nuestro \texttt{set} ordenado por $y$ ya no es correcto, pues el comparador de este cambia con el tiempo dependiendo de $x$, tendríamos que manualmente extraer e insertar los elementos necesarios.
  \item Si hay casos degenerados con tres segmentos o más que se intersectan en el mismo punto, hay que ser muy cuidadosos en que orden se procesan y cómo actualizamos la estructura usada.
  \item Si no sabemos con certeza una cota superior para la cantidad de intersecciones y se dé el peor caso ($n^2$), puede suceder que el algoritmo con complejidad $O(n^2logn)$ sea más lento que la fuerza bruta que era $O(n^2)$.
\end{itemize}
Usualmente no se necesita implementar Bentley Oattmann en concursos, ya que su implmentación son alrededor de 200 líneas con casos delicados de manejar, sería complicado encontrar bugs bajo pressión de tiempo, además que los problemas donde se requieren todas las intersecciones usualmente permiten fuerza bruta. Lo más usado es la idea en sí misma aplicado a problemas más sencillos como la unión de área de rectángulos como veremos en los ejemplos a continuación.

\section{Ejercicios}
\section{Problemas}